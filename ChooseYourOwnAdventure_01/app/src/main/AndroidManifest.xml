<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.palmdigital.chooseyourownadventure_01">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WakeUpActivity" />
        <activity android:name=".ExploreActivity" />
        <activity android:name=".pourIntoBackyardActivity" />
        <activity android:name=".pourIntoToiletActivity" />
        <activity android:name=".SoupActivity"></activity>
    </application>

</manifest>